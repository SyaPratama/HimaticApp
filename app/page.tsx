import Navbar from "@/components/navbar";
import HeroSection from "@/containers/landing-page/hero-section";

const Page = () => {
  return(
    <main>
      <Navbar/>
      <HeroSection/>
    </main>
  );
}

export default Page
